# Subject: retained previous form

`subject-square-triangle.svg` is the exact earlier selected Subject / נושא drawing: a solid square and upright triangle above a baseline. It is retained at the user’s explicit request, in case it proves more intuitive than the newer two-part statement tile.

The active selected asset remains `icons/ch11-subjects/subject-bearer.svg`, with its enlarged back cell and smaller translucent front. Do not switch automatically. If the user later prefers this earlier form, restore its geometry to the active key and update the generator, hash, guide, reference, and export together.

This retained alternative is part of the full handoff and has a preview/download in the Subject/Predicate review. It is not a new construct, not an additional selected key, and not a rejected drawing to be discarded. It stays outside the selected-only SVG export. The general no-old-variants packaging rule has this explicit user-authorized exception.

Original SVG SHA-256: `84f3518e89a54a69c2c51dbae76b74b4603a50d808d7a3aab39c7d90811a36c8`.
